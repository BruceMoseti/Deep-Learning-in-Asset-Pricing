# Exact versions used to produce the numbers in reports/RESEARCH_REPORT.md.
# pyproject.toml carries looser floors for installation; this file pins the
# environment, since results depend on library versions as well as on data
# vintage.
numpy==2.4.4
pandas==3.0.5
scipy==1.18.1
scikit-learn==1.9.0
statsmodels==0.15.0
xgboost==3.4.1
torch==2.14.0
matplotlib==3.11.1
pyarrow==25.0.1
tabulate==0.10.0
pytest==9.1.1
